
export function generatePuzzle() {
  
}

/**
 * Update item attached to mouse when mouse clicked
 * @param {*} mousepos mouse position
 */
export function onClicked(mousepos) {
  // if already attacked
  // check if placeable, yes => place, no => return previous position
  // else 
  // check if on shape, yes => attach shape, no => do nothing
}

/**
 * Update current shape position based on mouse position
 * @param {*} mousepos mouse position
 */
export function snapShape(mousepos) {
  // if shape snapped, update shape to mouse pos
}